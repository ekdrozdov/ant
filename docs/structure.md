## Body

Defines organizm properties, a set of interactions with the world and lifecycle. Controlled by an agent.

## Agent

Defines a task graph. Task uses host's api to interact with the world. Example: `Worker`.
